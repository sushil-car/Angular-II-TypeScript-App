import { QuantityPriceViewDirective } from './quantity-price-view.directive';

describe('QuantityPriceViewDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityPriceViewDirective();
    expect(directive).toBeTruthy();
  });
});
